[1mdiff --git a/src/main.cpp b/src/main.cpp[m
[1mindex 4e2330f..e354793 100644[m
[1m--- a/src/main.cpp[m
[1m+++ b/src/main.cpp[m
[36m@@ -1,42 +1,35 @@[m
[32m+[m[32m#include "minisql/index/bplus_tree.hpp"[m
 #include <iostream>[m
[31m-#include "minisql/catalog/row.hpp"[m
[31m-#include "minisql/catalog/schema.hpp"[m
 [m
[31m-int main()[m
[31m-{[m
[31m-    // Define a schema : a 'users' table with 3 Columns[m
[31m-    std::vector<minisql::Column> columns = {[m
[31m-        minisql::Column("id", minisql::ColumnType::INTEGER),[m
[31m-        minisql::Column("name", minisql::ColumnType::VARCHAR, 50),[m
[31m-        minisql::Column("is_active", minisql::ColumnType::BOOLEAN)};[m
[31m-    minisql::Schema schema(columns);[m
[32m+[m[32mint main(){[m
[32m+[m[32m    minisql::BPlusTree tree;[m
 [m
[31m-    std::cout << "Row size for this Schema : " << schema.GetRowSize() << " bytes\n";[m
[32m+[m[32m    // Insert enough keys to force multiple leaf splits and at least one internal node split[m
[32m+[m[32m    //(the Cascade that we just built).[m
[32m+[m[32m    for(int64_t i = 1; i <= 20; i++){[m
[32m+[m[32m        tree.insert(i*10, i*100);[m
[32m+[m[32m    }[m
 [m
[31m-    // Build one real row of data matching that schema.[m
[31m-    std::vector<minisql::Value> values = {[m
[31m-        minisql::Value(minisql::ColumnType::INTEGER, int64_t(101)),[m
[31m-        minisql::Value(minisql::ColumnType::VARCHAR, std::string("Vivek")),[m
[31m-        minisql::Value(minisql::ColumnType::BOOLEAN, true)};[m
[31m-    minisql::Row row(values);[m
[32m+[m[32m    int64_t result;[m
[32m+[m[32m    bool all_correct = true;[m
 [m
[31m-    // Serialize it into raw buffer.[m
[31m-    char buffer[256]; // storage[m
[31m-    row.SerializeTo(buffer, schema);[m
[31m-    std::cout << " Row serialized successfully. \n";[m
[32m+[m[32m    for(int64_t i = 1; i <= 20; i++){[m
[32m+[m[32m        int64_t key = i * 10;[m
[32m+[m[32m        int64_t expected = i * 100;[m
[32m+[m[32m        bool found = tree.Search(key, &result);[m
 [m
[31m-    // Deserialized it back into a new row.[m
[31m-    minisql::Row restored = minisql::Row::DeserializeFrom(buffer, schema);[m
[32m+[m[32m        if(!found || result != expected){[m
[32m+[m[32m            std::cout<<"MISMATCH at key " << key << "\n";[m
[32m+[m[32m            all_correct = false;[m
[32m+[m[32m        }[m
[32m+[m[32m    }[m
 [m
[31m-    // Print and Verify each value.[m
[31m-    const auto &restored_values = restored.GetValues();[m
[31m-    std::cout << "id : " << restored_values[0].GetInteger() << "\n";[m
[31m-    std::cout << "name : " << restored_values[1].GetString() << "\n";[m
[31m-    std::cout << "is_active " << (restored_values[2].GetBoolean() ? "true" : "false") << "\n";[m
[32m+[m[32m    bool found_missing = tree.Search(999, &result);[m
 [m
[31m-    bool ok = restored_values[0].GetInteger() == 101 && restored_values[1].GetString() == "Vivek" && restored_values[2].GetBoolean() == true;[m
[32m+[m[32m    std::cout<< (all_correct ? "SUCCESS : all 20 keys correct.\n" : "FAILURE: see mismatches above.\n");[m
 [m
[31m-    std::cout << (ok ? "SUCCESS : row round-tripped correctly. \n" : "FAILURE : mismatch after deserialization. \n");[m
[32m+[m[32m    std::cout<<"Search for non existent key 999 : "<< (found_missing ? "FOUND (unexpected)" : "NOT FOUND (correct)") << "\n";[m
 [m
     return 0;[m
[32m+[m
 }[m
\ No newline at end of file[m
